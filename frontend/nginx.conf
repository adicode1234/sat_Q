server { listen 80; root /usr/share/nginx/html; client_max_body_size 2600m;
location /api/ { proxy_pass http://gateway:8000; proxy_http_version 1.1; proxy_set_header Upgrade $http_upgrade; proxy_set_header Connection "upgrade"; proxy_read_timeout 300s; }
location /reports/ { proxy_pass http://gateway:8000; }
location / { try_files $uri /index.html; }
}
